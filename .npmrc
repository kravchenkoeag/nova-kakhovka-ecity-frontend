# .npmrc (в корені проекту)

# Hoisting configuration - це критично важливо для React
# Це гарантує що React буде встановлений тільки один раз на рівні root
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*next-auth*
public-hoist-pattern[]=@types/*

# Auto install peers
auto-install-peers=true

# Strict peer dependencies
strict-peer-dependencies=false

# Shamefully hoist (якщо проблема залишається)
shamefully-hoist=false

# Node linker
node-linker=isolated